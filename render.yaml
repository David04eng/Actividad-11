services:
  - type: web
    name: actividad-11-demo
    env: java
    plan: free
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/demo-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_OPTS
        value: -Dserver.port=8080
      - key: PORT
        value: 8080
